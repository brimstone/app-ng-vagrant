#!/bin/sh
echo '$*:' "$*" >> /tmp/ssh.log
if [ "$1" = "" ]; then
	exit
fi

while read -r aline; do
	echo "$aline" >> /tmp/ssh.log
	case "$aline" in
	"exit")
		exit
		;;
	"which rsync")
		echo ""
		;;
	"echo; printf /vagrant")
		printf "/vagrant"
		;;
	*)
	;;
	esac
done
